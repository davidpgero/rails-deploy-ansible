---

- import_playbook: bootstrap.yml

- name: Provision
  hosts: all
  remote_user: ansible
  become: yes
  roles:
    - role: cron
      tags: cron
    - role: time
      tags: time
    - role: git
      tags: git
    - role: ssh
      tags: ssh
    - role: ufw
      tags: ufw
    - role: packages
      tags: packages
    - role: user
      param_name: ubuntu
      param_encrypted_password: "{{ encrypted_ubuntu_password }}"
      tags: user_ubuntu
    - role: user
      param_name: app
      param_comment: Application User
      param_sudo: yes
      tags: user_app
    - role: user
      param_name: gdp
      param_comment: David Gero
      param_encrypted_password: "{{ encrypted_gdp_password }}"
      param_sudo: yes
      tags: user_gdp
